# Overwatch Queue Guard

Expect:

- Queue lists recent `dispatch` tasks and refreshes.
- Clicking an item fetches `/v1/overwatch/item?id=...`.
- Preview shows STB-B & STB-A with buttons to copy either; copy logs `ow-copy`.
- New controls: Runner select (codex/copilot/api) + Dry-run + Dispatch.
- Dispatch posts to `/v1/executors/dispatch` and a job box appears with logs (`begin`, `apply`, `run acceptance`, `result: ok`).
